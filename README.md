
# Apptivate Unified App — Updated UI

This bundle updates the frontend UI for a friendlier, attractive design and
formats AI output so users can copy/use it directly.

Features:
- Clean card-based UI
- Copy buttons for captions, hashtags, image prompt and full JSON
- Formatted display of results
- Responsive layout

Deployment:
- Same as before: set OPENAI_API_KEY in Vercel environment variables and deploy.

